node_modules/
dist/
.DS_Store
*.tsbuildinfo
.env*
coverage/
test/screenshots/*.current.png

# Playwright run artifacts (reports, traces, failure screenshots). The visual
# BASELINES under e2e/visual/__screenshots__ are committed; these are not.
playwright-report/
test-results/
blob-report/

# lookout evidence (screenshots; regenerable). The config, rubric, backlog,
# and ledger under .lookout/ ARE committed.
.lookout/evidence/

# Local Claude Code session state
.claude/

# Generated at pack time by tools/licensegen: the MIT grant covers the PUBLISHED
# package only. Committing it here would make GitHub read the whole repository as
# MIT, which is the opposite of the intent (source stays all rights reserved).
LICENSE
