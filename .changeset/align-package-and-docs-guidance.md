---
"@nannier-com/canvas": patch
---

Correct optional dependency fallbacks, source development instructions, repository links, privacy copy and the distinct licenses for the npm package and repository source. Document the validated artifact release process and route the release script through its CI-only guard.
