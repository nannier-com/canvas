---
"@nannier-com/canvas": patch
---

Wait for the native starter home screen before opening the first smoke route, preventing Android startup from dropping the verification link. Store screenshots under Maestro's run output directory using its supported relative paths. Select native Listbox rows by their accessible names between surrounding controls, supporting native layout flattening without relying on DOM ancestry.
