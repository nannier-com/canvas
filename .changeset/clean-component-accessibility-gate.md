---
"@nannier-com/canvas": patch
---

Remove the repaired component accessibility exceptions. Every component page now uses the same serious/critical violation gate, with raw diagnostic evidence retained for failures.
