---
"@nannier-com/canvas": patch
---

Prepare release versions before validating a frozen candidate, then publish only its tested npm tarball and docs archive. Reject stale candidates without rebasing or publishing, enforce the major-version guard for manual and automatic runs, and report actual delivery outcomes. Prepare Cloudflare asset paths in the shared docs build before browser tests.
