---
"@nannier-com/canvas": patch
---

Keep optional blur types out of public declarations so strict TypeScript consumers can omit expo-blur. Add isolated packed-consumer checks for React 18.0 on the web, the React Native 0.74 and React 18.2 host pair, and the current locked peers, including real web interaction checks and stock Metro iOS/Android bundles with optional peers absent.
