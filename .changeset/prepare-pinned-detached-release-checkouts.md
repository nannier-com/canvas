---
"@nannier-com/canvas": patch
---

Provide Changesets its local main reference when CI checks out a pinned commit in detached mode. Keep the reference at the triggering source SHA and validate the resulting version candidate before delivery.
