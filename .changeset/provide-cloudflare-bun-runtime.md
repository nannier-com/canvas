---
"@nannier-com/canvas": patch
---

Install the Bun runtime in the Cloudflare delivery job so Wrangler can deploy the validated web archive using the docs workspace's package manager. This new patch follows the successful 2.60.4 npm publication and its failed web delivery.
