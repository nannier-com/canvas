---
"@nannier-com/canvas": patch
---

Give each FilterPanel option one accessible checkbox and tab stop by sharing noninteractive Checkbox content. Space toggles on release, while Enter, row clicks, labels and counts retain one shared selection path and each platform keeps its existing visual anatomy.
