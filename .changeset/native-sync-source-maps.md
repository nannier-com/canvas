---
"@nannier-com/canvas": patch
---

Update registered local consumer overlays with the package's native entry metadata after its compiled targets exist, preserving package versions and dependency ranges. Retain exact source-map positions when native builds shorten relative module requests for Metro platform resolution.
