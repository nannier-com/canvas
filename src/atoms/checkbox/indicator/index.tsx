import { createCheckboxIndicator } from "./shared.js";
import { webSkin } from "../checkbox.styles.js";

// Internal only, deliberately absent from the public Checkbox and kit barrels.
export const CheckboxIndicator = createCheckboxIndicator(webSkin);
