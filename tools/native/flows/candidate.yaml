appId: com.nannier.canvas.starter.smoke
name: Sealed Canvas native candidate
---
- launchApp:
    clearState: true
# The router must mount before Android can deliver a warm-start link.
- assertVisible: Your workspace
- openLink: canvas-smoke:///testing/diagnostics
# iOS can require first-use confirmation before opening the app's custom scheme.
- runFlow:
    when:
      platform: iOS
      visible: 'Open in “Canvas Starter Smoke”\?'
    commands:
      - tapOn: Open
- assertVisible: Candidate runtime identity
- assertVisible:
    id: smoke-input-mode
    text: package
- assertVisible:
    id: smoke-appearance
    text: ${SCHEME}
- assertVisible:
    id: smoke-candidate-revision
    text: ${CANDIDATE}
- assertVisible:
    id: smoke-package-sha256
    text: ${PACKAGE_SHA256}
- scrollUntilVisible:
    element:
      id: smoke-package-version
    direction: DOWN
- assertVisible:
    id: smoke-package-version
    text: ${PACKAGE_VERSION}
- takeScreenshot: identity
- scrollUntilVisible:
    element:
      id: smoke-bundle-url
    direction: DOWN
- assertVisible:
    id: smoke-bundle-url
    text: (file:|assets:|/).*
- openLink: canvas-smoke:///testing/control-refs
- assertVisible: Control refs
- tapOn: Inspect refs
- assertVisible:
    id: ref-attached
    text: 'Attached: 6'
- tapOn: Measure Slider
- assertVisible:
    id: ref-measurement
    text: 'Measured: [1-9][0-9]* × [1-9][0-9]*'
- tapOn: Focus Button
- scrollUntilVisible:
    element:
      text: Focus Checkbox
    direction: UP
- tapOn: Focus Checkbox
- scrollUntilVisible:
    element:
      text: Focus Switch
    direction: UP
- tapOn: Focus Switch
- scrollUntilVisible:
    element:
      text: Focus Radio
    direction: UP
- tapOn: Focus Radio
- scrollUntilVisible:
    element:
      text: Focus Select
    direction: UP
- tapOn: Focus Select
- scrollUntilVisible:
    element:
      text: Focus Slider
    direction: UP
- tapOn: Focus Slider
- scrollUntilVisible:
    element:
      id: ref-requested
    direction: UP
- assertVisible:
    id: ref-requested
    text: 'Requested: Slider'
- assertVisible:
    id: ref-changes
    text: 'Changes: 0'
- assertNotVisible: Ref dialog
- assertNotVisible: Pear
- takeScreenshot: refs-measured-without-activation
- scrollUntilVisible:
    element:
      text: Unmount controls
    direction: UP
- tapOn: Unmount controls
- tapOn: Inspect refs
- assertVisible:
    id: ref-attached
    text: 'Attached: 0'
- tapOn: Mount controls
- tapOn: Inspect refs
- assertVisible:
    id: ref-attached
    text: 'Attached: 6'
# Exercise an unchecked Radio before the other remounted controls. These are
# ordinary taps; spoken-feedback clickability is a separate manual journey.
- scrollUntilVisible:
    element:
      text: Weekly
    direction: DOWN
- tapOn:
    text: Weekly
    retryTapIfNoChange: false
- scrollUntilVisible:
    element:
      id: ref-changes
    direction: UP
- assertVisible:
    id: ref-changes
    text: 'Changes: 1'
- scrollUntilVisible:
    element:
      text: Disable controls
    direction: UP
- tapOn: Disable controls
- scrollUntilVisible:
    element:
      id: ref-radio
    direction: DOWN
- tapOn:
    id: ref-radio
    retryTapIfNoChange: false
- scrollUntilVisible:
    element:
      id: ref-changes
    direction: UP
- assertVisible:
    id: ref-changes
    text: 'Changes: 1'
- takeScreenshot: radio-disabled-inert
- scrollUntilVisible:
    element:
      text: Enable controls
    direction: UP
- tapOn: Enable controls
- scrollUntilVisible:
    element:
      id: ref-radio
    direction: DOWN
- tapOn:
    id: ref-radio
    retryTapIfNoChange: false
- scrollUntilVisible:
    element:
      id: ref-changes
    direction: UP
- assertVisible:
    id: ref-changes
    text: 'Changes: 2'
- takeScreenshot: radio-changed-once-per-tap
- scrollUntilVisible:
    element:
      id: ref-checkbox
    direction: DOWN
- tapOn:
    id: ref-checkbox
- scrollUntilVisible:
    element:
      id: ref-switch
    direction: DOWN
- tapOn:
    id: ref-switch
- scrollUntilVisible:
    element:
      id: ref-changes
    direction: UP
- assertVisible:
    id: ref-changes
    text: 'Changes: 4'
- takeScreenshot: refs-remounted-and-interactive
- openLink: canvas-smoke:///testing/form-autocomplete
- assertVisible: Form and autocomplete
- tapOn:
    id: fruit-input
- inputText: Ap
- assertVisible: Apricot
- takeScreenshot: autocomplete-open
- tapOn: Apricot
- hideKeyboard
- scrollUntilVisible:
    element:
      id: selection-count
    direction: DOWN
- assertVisible:
    id: fruit-value
    text: Apricot
- assertVisible:
    id: selection-count
    text: '1'
- assertVisible:
    id: submit-count
    text: '0'
- tapOn: Save choice
- assertVisible:
    id: submit-count
    text: '1'
- openLink: canvas-smoke:///testing/listbox
- assertVisible: Listbox input checks
- scrollUntilVisible:
    element:
      id: primary-team
    direction: DOWN
- tapOn:
    text: Frontend, Web applications
    above: Multiple selections
    retryTapIfNoChange: false
- scrollUntilVisible:
    element:
      id: single-pick-count
    direction: DOWN
- assertVisible:
    id: single-change-count
    text: 'Changes: 1'
- assertVisible:
    id: single-pick-count
    text: 'Picks: 1'
- takeScreenshot: listbox-single-selected
- scrollUntilVisible:
    element:
      id: project-teams
    direction: DOWN
# Native layout flattening can expose a group and its rows as siblings.
# Scope the named row between the fixture's surrounding controls.
- tapOn:
    text: Frontend, Web applications
    below: Before teams
    above: After teams
- scrollUntilVisible:
    element:
      id: multi-pick-count
    direction: DOWN
- assertVisible:
    id: multi-change-count
    text: 'Changes: 1'
- assertVisible:
    id: multi-pick-count
    text: 'Picks: 1'
- takeScreenshot: listbox-selected
- scrollUntilVisible:
    element:
      id: project-teams
    direction: UP
- tapOn:
    text: Frontend, Web applications
    below: Before teams
    above: After teams
- scrollUntilVisible:
    element:
      id: multi-change-count
    direction: DOWN
- assertVisible:
    id: multi-change-count
    text: 'Changes: 2'
- assertVisible:
    id: multi-pick-count
    text: 'Picks: 2'
- openLink: canvas-smoke:///testing/listbox?disabled=true
- assertVisible: Listbox input checks
- scrollUntilVisible:
    element:
      id: primary-team
    direction: DOWN
- tapOn:
    text: Frontend, Web applications
    above: Multiple selections
    retryTapIfNoChange: false
- scrollUntilVisible:
    element:
      id: single-pick-count
    direction: DOWN
- assertVisible:
    id: single-change-count
    text: 'Changes: 0'
- assertVisible:
    id: single-pick-count
    text: 'Picks: 0'
- scrollUntilVisible:
    element:
      id: project-teams
    direction: DOWN
- tapOn:
    text: Frontend, Web applications
    below: Before teams
    above: After teams
    retryTapIfNoChange: false
- scrollUntilVisible:
    element:
      id: multi-pick-count
    direction: DOWN
- assertVisible:
    id: multi-change-count
    text: 'Changes: 0'
- assertVisible:
    id: multi-pick-count
    text: 'Picks: 0'
- takeScreenshot: listbox-disabled-inert
- openLink: canvas-smoke:///testing/tabs
- assertVisible: Tabs input checks
- assertVisible:
    id: tabs-selection
    text: 'Selected tab: Overview'
- tapOn:
    text: Activity
    retryTapIfNoChange: false
- assertVisible:
    id: tabs-selection
    text: 'Selected tab: Activity'
- assertVisible:
    id: tabs-change-count
    text: 'Changes: 1'
- tapOn:
    text: Unavailable
    retryTapIfNoChange: false
- assertVisible:
    id: tabs-selection
    text: 'Selected tab: Activity'
- assertVisible:
    id: tabs-change-count
    text: 'Changes: 1'
- takeScreenshot: tabs-inactive-selected-disabled-inert
- openLink: canvas-smoke:///testing/tabs?disabled=true
- assertVisible: Tabs input checks
- tapOn:
    text: Activity
    retryTapIfNoChange: false
- assertVisible:
    id: tabs-selection
    text: 'Selected tab: Overview'
- assertVisible:
    id: tabs-change-count
    text: 'Changes: 0'
- takeScreenshot: tabs-all-disabled-inert
- openLink: canvas-smoke:///testing/escape-layers?scenario=drawer
- tapOn: Open drawer
- assertVisible: Drawer content
- tapOn: Open menu
- assertVisible: Archive
- takeScreenshot: drawer-menu-before-archive
- tapOn:
    text: Archive
    retryTapIfNoChange: false
- assertNotVisible: Rename
- assertVisible: Drawer content
- assertVisible:
    id: menu-selected
    text: 'Selected: Archive'
- assertVisible:
    id: menu-select-count
    text: 'Selections: 1'
- takeScreenshot: drawer-archive-selected-once
- openLink: canvas-smoke:///testing/escape-layers?scenario=drawer-disabled
- tapOn: Open drawer
- tapOn: Open menu
- assertVisible: Archive
- tapOn:
    text: Archive
    retryTapIfNoChange: false
- assertVisible: Rename
- assertVisible: Archive
# The outside backdrop at the trigger closes the child without selecting.
- tapOn:
    text: Open menu
    retryTapIfNoChange: false
- assertNotVisible: Rename
- assertVisible: Drawer content
- assertVisible:
    id: menu-selected
    text: 'Selected: None'
- assertVisible:
    id: menu-select-count
    text: 'Selections: 0'
- takeScreenshot: drawer-archive-disabled-inert
- openLink: canvas-smoke:///testing/form-autocomplete?scenario=modal
- tapOn: Open fruit drawer
- takeScreenshot: drawer-field-before-keyboard
- tapOn:
    id: drawer-fruit-input
- inputText: Ap
- assertVisible: Apricot
- takeScreenshot: drawer-hosted-options
- tapOn: Apricot
- assertVisible:
    id: drawer-fruit-input
    text: Apricot
- assertNotVisible: Apple
- takeScreenshot: drawer-selection
# Submit the active iOS input through its real keyboard action. A generic
# dismiss gesture cannot reach a keyboard inside this native Modal reliably.
- runFlow:
    when:
      platform: iOS
    commands:
      - tapOn:
          id: Return
      - assertNotVisible:
          id: space
- runFlow:
    when:
      platform: Android
    commands:
      - hideKeyboard
- openLink: canvas-smoke:///testing/form-autocomplete?scenario=action-sheet-safe-area
- tapOn: Open safe area sheet
- assertVisible: Safe area actions
- assertVisible: Choose safe area action
- takeScreenshot: action-sheet-bottom-clearance
- tapOn: Choose safe area action
- assertVisible:
    id: sheet-selections
    text: 'Selections: 1'
- assertVisible:
    id: sheet-closes
    text: 'Closes: 1'
- tapOn: Open safe area sheet
- assertVisible: Choose safe area action
- takeScreenshot: action-sheet-cancel-clearance
# The full-window scrim shares the Cancel name. Select the actual bottom row.
- tapOn:
    text: Cancel safe area sheet
    below: Choose safe area action
- assertVisible:
    id: sheet-selections
    text: 'Selections: 1'
- assertVisible:
    id: sheet-closes
    text: 'Closes: 2'
# This scenario forces glass inside the inherited light/dark scheme. Use the
# built-in Dialog body rather than custom children so description/currency paint.
- openLink: canvas-smoke:///testing/escape-layers?scenario=glass-messages
- assertVisible: Overlay keyboard checks
- tapOn:
    text: Open glass message dialog
    retryTapIfNoChange: false
- assertVisible: Glass refund details
- assertVisible: Check the refund amount and reason before continuing.
- assertVisible: Amount
- assertVisible: Reason
- assertVisible: '^\$$'
- takeScreenshot: glass-dialog-built-in-message
- tapOn:
    text: Cancel
    retryTapIfNoChange: false
- assertNotVisible: Glass refund details
- tapOn:
    text: Open glass message sheet
    retryTapIfNoChange: false
- assertVisible: Glass sharing actions
- assertVisible: Choose a sharing action for this document.
- assertVisible: Copy document link
- takeScreenshot: glass-action-sheet-header
- tapOn:
    text: Close sheet
    below: Copy document link
    retryTapIfNoChange: false
- assertNotVisible: Glass sharing actions
- openLink: canvas-smoke:///testing/carousel
- assertVisible: Native carousel paging
- assertVisible:
    id: carousel-current
    text: 'Current page: 2'
- assertVisible:
    id: carousel-changes
    text: 'Page changes: 0'
- assertVisible: Slide 2 of 6, current slide
- assertVisible:
    id: carousel-page-2
- takeScreenshot: carousel-default-page
- assertVisible:
    id: carousel-measurement
    text: '.*"generation":0,"status":"idle".*'
- tapOn: Measure current slide
- assertVisible:
    id: carousel-measurement
    text: '.*"generation":1,"status":"ready".*'
- copyTextFrom:
    id: carousel-measurement
# These task apps use a full-screen, edge-to-edge native window. RN window
# coordinates therefore share the screen origin. Maestro starts at 80%,50% of
# the selected Card and its LEFT driver ends at 10% of the screen width. It uses
# iOS points and Android physical pixels, so only Android applies PixelRatio.
# The phone layout must place the card across that 10% line; a wider gutter is
# rejected because the driver's endpoint would leave the card.
# Reject stale, clipped, zero-sized or wrong-card measurements before touching.
- evalScript: |
    ${output.carouselDrag = (function () {
      var m = JSON.parse(maestro.copiedText);
      if (m.generation !== 1 || m.status !== "ready" || m.index !== 1 || m.page !== 2)
        throw new Error("Expected a fresh measurement of current page 2");
      var fields = [m.x, m.y, m.width, m.height, m.screenWidth, m.screenHeight, m.pixelRatio];
      if (!fields.every(function (value) { return typeof value === "number" && isFinite(value); })
        || m.width <= 0 || m.height <= 0 || m.screenWidth <= 0 || m.screenHeight <= 0 || m.pixelRatio <= 0
        || m.x < 0 || m.y < 0 || m.x + m.width > m.screenWidth || m.y + m.height > m.screenHeight)
        throw new Error("Current card must have finite, positive bounds entirely inside the screen");
      if ((m.platform !== "ios" && m.platform !== "android") || m.rtl !== false)
        throw new Error("This journey requires an iOS or Android LTR candidate");
      var scale = m.platform === "android" ? m.pixelRatio : 1;
      var startX = (m.x + m.width * 0.8) * scale;
      var endX = m.screenWidth * scale * 0.1;
      var y = (m.y + m.height * 0.5) * scale;
      // Maestro quantizes native bounds, relative offsets and the screen edge
      // independently. Four driver units conservatively cover those roundings;
      // reject geometry where containment or travel depends on their direction.
      var roundingMargin = 4;
      if (startX - endX - roundingMargin * 2 <= m.width * scale / 2
        || endX - roundingMargin <= m.x * scale || startX + roundingMargin >= (m.x + m.width) * scale
        || y - roundingMargin <= m.y * scale || y + roundingMargin >= (m.y + m.height) * scale)
        throw new Error("The single drag must stay inside the card and cross more than half its width");
      return { startX: startX, endX: endX, y: y, roundingMargin: roundingMargin, measurement: m };
    })()}
- assertVisible:
    id: carousel-page-2
- assertVisible: Slide 2 of 6, current slide
- assertVisible:
    id: carousel-changes
    text: 'Page changes: 0'
- takeScreenshot: carousel-measured-before-drag
- swipe:
    direction: LEFT
    from:
      id: carousel-slide-2
      point: "80%, 50%"
    duration: 400
- assertVisible:
    id: carousel-current
    text: 'Current page: 3'
- assertVisible:
    id: carousel-changes
    text: 'Page changes: 1'
- assertVisible: Slide 3 of 6, current slide
- assertVisible:
    id: carousel-page-3
- takeScreenshot: carousel-swiped-page
- tapOn: Slide 6 of 6
- assertVisible:
    id: carousel-current
    text: 'Current page: 6'
- assertVisible:
    id: carousel-changes
    text: 'Page changes: 2'
- assertVisible: Slide 6 of 6, current slide
- assertVisible:
    id: carousel-page-6
- takeScreenshot: carousel-picker-jump
# The diagnostic retains the actual visible host band after keyboard dismissal.
# Keep the keyboard open until the previously obscured final match is selected.
- openLink: canvas-smoke:///testing/form-autocomplete?scenario=keyboard
- scrollUntilVisible:
    element:
      id: overlay-visible-measurement
      text: 'Visible: [1-9][0-9]* to [1-9][0-9]*'
    direction: DOWN
- takeScreenshot: autocomplete-visible-band-before-keyboard
- scrollUntilVisible:
    element:
      id: fruit-input
    direction: UP
- tapOn:
    id: fruit-input
- inputText: Ap
- scrollUntilVisible:
    element:
      text: Pineapple
    direction: DOWN
- takeScreenshot: autocomplete-final-match-with-keyboard
- tapOn: Pineapple
- assertVisible:
    id: fruit-input
    text: Pineapple
- hideKeyboard
- scrollUntilVisible:
    element:
      id: selection-count
    direction: DOWN
- assertVisible:
    id: selection-count
    text: '1'
- assertVisible:
    id: submit-count
    text: '0'
- scrollUntilVisible:
    element:
      id: overlay-resize-observation
    direction: DOWN
- runFlow:
    when:
      platform: Android
    commands:
      - assertVisible:
          id: overlay-resize-observation
          text: Visible overlay height shrank
- takeScreenshot: autocomplete-visible-band-after-selection
- openLink: canvas-smoke:///testing/form-autocomplete?scenario=modal-keyboard
- tapOn: Open fruit drawer
- assertVisible:
    id: overlay-visible-measurement
    text: 'Visible: [1-9][0-9]* to [1-9][0-9]*'
- takeScreenshot: drawer-visible-band-before-keyboard
- tapOn:
    id: drawer-fruit-input
- inputText: Ap
- scrollUntilVisible:
    element:
      text: Pineapple
    direction: DOWN
- takeScreenshot: drawer-final-match-with-keyboard
- tapOn: Pineapple
- assertVisible:
    id: drawer-fruit-input
    text: Pineapple
# Submit the active iOS input through its real keyboard action. A generic
# dismiss gesture cannot reach a keyboard inside this native Modal reliably.
- runFlow:
    when:
      platform: iOS
    commands:
      - tapOn:
          id: Return
      - assertNotVisible:
          id: space
- runFlow:
    when:
      platform: Android
    commands:
      - hideKeyboard
- runFlow:
    when:
      platform: Android
    commands:
      - assertVisible:
          id: overlay-resize-observation
          text: Visible overlay height shrank
- takeScreenshot: drawer-visible-band-after-selection
- openLink: canvas-smoke:///testing/form-autocomplete?scenario=command-keyboard
- scrollUntilVisible:
    element:
      id: overlay-visible-measurement
      text: 'Visible: [1-9][0-9]* to [1-9][0-9]*'
    direction: DOWN
- scrollUntilVisible:
    element:
      id: keyboard-command
    direction: UP
- tapOn: Search...
# Opening the actual mounted card must focus its input without another tap.
- runFlow:
    when:
      platform: Android
    commands:
      - assertVisible:
          text: Search work items
          focused: true
# iOS can report focused=false for an active native text host. Actual typing
# without another tap plus the visible software space key verifies input focus.
- inputText: Work
- assertVisible: Work
- runFlow:
    when:
      platform: iOS
    commands:
      - assertVisible:
          id: space
# Native hierarchy visibility does not account for clipping by the results pane.
# Require the last row above the actual footer before selecting it.
- scrollUntilVisible:
    element:
      text: Work item 40
      above: to navigate
    direction: DOWN
- runFlow:
    when:
      platform: Android
    commands:
      - assertVisible:
          text: Search work items
          focused: true
- runFlow:
    when:
      platform: iOS
    commands:
      - assertVisible:
          id: space
- assertVisible: to navigate
- assertVisible: to select
- assertVisible: to close
- takeScreenshot: command-final-result-with-keyboard
# Typing after a real results drag proves that input focus was preserved.
- inputText: ' item'
- assertVisible:
    id: command-query
    text: 'Query: Work item'
- runFlow:
    when:
      platform: iOS
    commands:
      - assertVisible:
          id: space
- scrollUntilVisible:
    element:
      text: Work item 40
      above: to navigate
    direction: DOWN
- runFlow:
    when:
      platform: Android
    commands:
      - assertVisible:
          text: Search work items
          focused: true
- runFlow:
    when:
      platform: iOS
    commands:
      - assertVisible:
          id: space
- assertVisible: to navigate
- assertVisible: to select
- assertVisible: to close
- takeScreenshot: command-before-final-selection
- tapOn:
    text: Work item 40
    above: to navigate
# Observe search input closure after selection. A generic hideKeyboard can
# send Android Back when the keyboard is already gone and leave this route.
- assertNotVisible: Search work items
- scrollUntilVisible:
    element:
      id: command-selected-index
    direction: DOWN
- assertVisible:
    id: command-query
    text: 'Query: Work item'
- assertVisible:
    id: command-selected
    text: 'Selected: Work item 40'
- assertVisible:
    id: command-selected-index
    text: 'Selected index: 39'
- assertVisible:
    id: command-selections
    text: 'Selections: 1'
- scrollUntilVisible:
    element:
      id: overlay-resize-observation
    direction: DOWN
- runFlow:
    when:
      platform: Android
    commands:
      - assertVisible:
          id: overlay-resize-observation
          text: Visible overlay height shrank
- takeScreenshot: command-selected-with-preserved-query
